<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for all API calls (backend, image loading) and for
         google_fonts, which fetches font files over the network at runtime. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Location permissions (ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION)
         are intentionally NOT declared yet. `geolocator` is a pubspec
         dependency but is not called from anywhere in lib/ today. Add them
         here, together with the corresponding runtime permission request
         flow, only when a location feature actually ships — declaring them
         earlier adds unnecessary Play Store data-safety disclosure and
         review friction.

         CAMERA / READ_MEDIA_IMAGES / READ_EXTERNAL_STORAGE are also
         intentionally NOT declared. image_picker on Android delegates to
         the system Photo Picker / share-sheet intents, which need none of
         these; declaring CAMERA in particular would force a runtime
         permission prompt that the app does not otherwise need. -->

    <application
        android:label="1Cup"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
