# NOTE: gradle-wrapper.jar, gradlew and gradlew.bat are intentionally NOT
# ignored — the wrapper must be committed so CI and other devs can build
# without a system-wide Gradle install (see CLAUDE.md / phase-11 task notes).
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
