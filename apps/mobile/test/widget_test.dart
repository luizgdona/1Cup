// Baseline smoke test — confirms the test harness runs cleanly.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test harness is operational', () {
    expect(1 + 1, equals(2));
  });
}
